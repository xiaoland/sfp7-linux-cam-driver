# The IPU4 driver exposes many internal video nodes that are not desktop
# cameras.  Let libcamera own the media graph and keep the raw V4L2 monitor
# from probing and advertising those internal nodes in user sessions.
wireplumber.profiles = {
  main = {
    monitor.v4l2 = disabled
  }
}
